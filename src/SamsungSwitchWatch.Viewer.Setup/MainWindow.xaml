<Window x:Class="SamsungSwitchWatch.Viewer.Setup.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Samsung Switch Watch Viewer 설치"
        Width="700"
        Height="560"
        MinWidth="620"
        MinHeight="500"
        FontFamily="Noto Sans KR, Segoe UI"
        Foreground="{StaticResource TextBrush}"
        Background="{StaticResource CanvasBrush}"
        TextOptions.TextFormattingMode="Display"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/SetupResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock FontSize="24"
                       FontWeight="SemiBold"
                       Text="Samsung Switch Watch Viewer" />
            <TextBlock Margin="0,6,0,0"
                       Foreground="{StaticResource MutedTextBrush}"
                       Text="관리자 권한이나 인터넷 연결 없이 현재 사용자 계정에 설치합니다." />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Ellipse x:Name="StatusIndicator"
                         Width="12"
                         Height="12"
                         Margin="0,2,12,0"
                         VerticalAlignment="Top"
                         Fill="#64748B" />
                <StackPanel Grid.Column="1">
                    <TextBlock x:Name="StatusTitle"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Text="설치 준비 완료" />
                    <TextBlock x:Name="StatusMessage"
                               Margin="0,4,0,0"
                               Foreground="{StaticResource MutedTextBrush}"
                               Text="설치 / 업데이트를 누르면 파일을 검증한 뒤 안전하게 교체합니다." />
                    <TextBlock x:Name="StatusCode"
                               Margin="0,6,0,0"
                               FontFamily="Consolas"
                               Foreground="{StaticResource MutedTextBrush}"
                               Visibility="Collapsed" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock FontWeight="SemiBold" Text="작업 내역" />
                <ListBox x:Name="StepList"
                         Grid.Row="1"
                         Margin="0,10,0,0"
                         BorderThickness="0"
                         Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,4">
                                <TextBlock FontWeight="SemiBold" Text="{Binding Label}" />
                                <TextBlock Margin="0,2,0,0"
                                           Foreground="{StaticResource MutedTextBrush}"
                                           Text="{Binding Message}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Button x:Name="InstallButton"
                        Style="{StaticResource PrimaryButton}"
                        Click="InstallButton_OnClick"
                        Content="설치 / 업데이트" />
                <Button x:Name="RecoverButton"
                        Margin="8,0,0,0"
                        Style="{StaticResource SecondaryButton}"
                        Click="RecoverButton_OnClick"
                        Content="이전 상태 복구"
                        IsEnabled="False" />
            </StackPanel>
            <Button Grid.Column="1"
                    x:Name="CloseButton"
                    Style="{StaticResource SecondaryButton}"
                    Click="CloseButton_OnClick"
                    Content="닫기" />
        </Grid>
    </Grid>
</Window>
